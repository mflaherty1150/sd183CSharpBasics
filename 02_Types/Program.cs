// bool is true and false, no quotes around the word
// this is because true and false are keywords, not strings
bool isLoggedIn = true;
Console.WriteLine(isLoggedIn);

// integers, or int, are whole numbers only
int age = 42;
Console.WriteLine(age);

// decimal has a lot of possible decimal points....
// it also need the m suffix to know it's a decimal type
decimal accountBalance = 9000.01m;

// float
float newFloat = 42.86f;

// dounble 
double newDouble = 36.49d;

var newNumber = 45.9;  // This will work because the system defaults to a double type

short newShort = 32767;
byte newByte = 255;

string anotherName = "My name is Anigo Montoya 2345.234 s";

char zulu = 'z';

string zulu2 = "z";

int? ni = null;